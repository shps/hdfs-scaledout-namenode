# Config file for collectd(1).

LoadPlugin exec

<Plugin exec>
        NotificationExec kthfs "/etc/collectd/alert.py"
</Plugin>

Include "/etc/collectd/filters.conf"
Include "/etc/collectd/thresholds.conf"

