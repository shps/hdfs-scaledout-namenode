#!/bin/bash

rootDir=<%= node[:kthfs][:dir] %>/hadoop
. $rootDir/sbin/scripts/set-env.sh $rootDir

HDFS=$HADOOP_COMMON_HOME/sbin/hadoop-daemon.sh

echo "Stopping the DataNode"
$HDFS stop datanode

exit $?

