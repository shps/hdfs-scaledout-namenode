#!/bin/sh 

MYSQL_SOCKET=`<%= @ndb_dir %>/scripts/get-mysql-socket.sh`

PID_FILE=<%= node[:ndb][:log_dir] %>/mysql_<%= node[:mysql][:id] %>.pid 
if [ ! -f $PID_FILE  ] ; then 
    # No pid file, assume mysql server is not running
    exit 0
fi
PID=`cat $PID_FILE`
kill -TERM $PID
RES=$?
if [ $RES -ne 0 ] ; then
 kill -9 $PID 
else
 exit 0
fi
exit $?

